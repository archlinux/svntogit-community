# $Id$
# Maintainer: Sergej Pupykin <pupykin.s+arch@gmail.com>
# Contributor: David Moore <davidm@sjsoft.com>

pkgname=aqbanking
pkgver=5.0.0
pkgrel=2
pkgdesc="A library for online banking and financial applications"
arch=(i686 x86_64)
options=('!libtool')
url="http://www.aquamaniac.de/aqbanking"
license=('GPL')
depends=('gwenhywfar' 'ktoblzcheck' 'libofx')
options=('!makeflags')
source=("aqbanking-$pkgver.tar.gz::http://www2.aquamaniac.de/sites/download/download.php?package=03&release=73&file=01&dummy=aqbanking-$pkgver.tar.gz")
md5sums=('57c640d370b1df2bc09a0b127e47bf36')

build() {
  cd $srcdir/aqbanking-$pkgver

  export PKG_CONFIG=/usr/bin/pkg-config
  ./configure --prefix=/usr \
	    --with-backends="aqhbci aqofxconnect" \
	    --enable-gwenhywfar

  make || return 1
  make DESTDIR=$pkgdir install
}
