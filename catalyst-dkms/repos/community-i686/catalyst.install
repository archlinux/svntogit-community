post_install() 
{
    dkms add -m fglrx -v $1
    dkms build -m fglrx -v $1
    dkms install -m fglrx -v $1
}

pre_upgrade() 
{
    dkms remove -m fglrx -v $1 --all
}

post_upgrade() 
{
    dkms add -m fglrx -v $1
    dkms build -m fglrx -v $1
    dkms install -m fglrx -v $1
}

pre_remove() 
{
    dkms remove -m fglrx -v $1 --all
}

