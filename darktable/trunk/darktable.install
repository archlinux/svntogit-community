post_install() {
  [ -x `which update-desktop-database 2>/dev/null` ] &&  update-desktop-database -q
}

post_upgrade() {
  [ -x `which update-desktop-database 2>/dev/null` ] &&  update-desktop-database -q
}

post_remove() {
  [ -x `which update-desktop-database 2>/dev/null` ] &&  update-desktop-database -q
}
