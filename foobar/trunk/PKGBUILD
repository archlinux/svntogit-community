pkgname=foobar
pkgver=1.0
pkgrel=1
pkgdesc="A stupid little test for the community DB scripts"
arch=(i686 x86_64)
url="http://sadtrombone.com"
license=('GPL')

build() {
  mkdir -p $pkgdir/usr/share/foobar
  echo "Hello!" > $pkgdir/usr/share/foobar/hi-there
}

# vim:set ts=2 sw=2 et:
