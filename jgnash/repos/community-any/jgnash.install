## arg 1:  the new package version
post_install() {
  echo 'jGnash is known to not work properly with the OpenJDK 6 Java environment.'
  echo 'Until upstream bugs are fixed, you should install the jre package to use jGnash.'
}

## arg 1:  the new package version
## arg 2:  the old package version
post_upgrade() {
  post_install $1
}
