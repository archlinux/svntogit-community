# $Id$
# Maintainer:  Biru Ionut <ionut@archlinux.ro>
# Contributor: Hugo Doria <hugo@archlinux.org>
# Contributor: Gour <Gour <gour@mail.inet.hr>

pkgname=libisoburn
pkgver=0.4.6.pl00
pkgrel=1
pkgdesc="frontend for libraries libburn and libisofs"
url="http://libburnia.pykix.org/wiki/Libisoburn"
arch=('i686' 'x86_64')
license=('GPL2')
depends=('libburn>=0.7.4' 'libisofs>=0.6.24' 'readline>=6.0')
options=('!libtool')
source=(http://files.libburnia-project.org/releases/${pkgname}-${pkgver}.tar.gz)

build() {
  cd ${srcdir}/${pkgname}-${pkgver/.pl??/}
  ./configure --prefix=/usr || return 1
  make || return 1
  make DESTDIR=${pkgdir} install || return 1
}
md5sums=('0d8e998eb78d772fda9c15d7a38d2ef0')
