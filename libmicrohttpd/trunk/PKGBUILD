# Maintainer: Norberto Lopes <shelika@mail.telepac.pt>
# Contributor: Kao Dome <kaodome@gmail.com>
# Contributor: Dmitrij D. Czarkoff <czarkoff@gmail.com>
# Contributor: Mathias Rohnstock <linksoft@gmx.de>

pkgname=libmicrohttpd
pkgver=0.9.3
pkgrel=1
pkgdesc="a small C library that is supposed to make it easy to run an HTTP server as part of another application."
arch=('i686' 'x86_64')
url="http://www.gnu.org/software/libmicrohttpd/"
license=('LGPL')
options=(!libtool)
depends=('gnutls')
source=(ftp://ftp.gnu.org/gnu/libmicrohttpd/$pkgname-$pkgver.tar.gz)
sha256sums=('23fce3b73d464a826d8ae619e665fe963c5a79af1b20a487572fd1e314d0d74c')

build() {
	cd ${pkgname}-${pkgver}
	./configure --prefix=/usr --disable-dependency-tracking --enable-largefile --enable-curl --enable-messages --with-pic
	make
}

package() {
	cd ${pkgname}-${pkgver}
	make DESTDIR=${pkgdir} install
	install -Dm644 ${srcdir}/${pkgname}-${pkgver}/src/include/platform.h \
		${pkgdir}/usr/include/platform.h
	install -Dm644 ${srcdir}/${pkgname}-${pkgver}/src/include/plibc/plibc.h \
		${pkgdir}/usr/include/plibc.h
}
sha256sums=('8913711b705bb951379a7652a4c19f861e088eb82007ae1860aba076520cb357')
