# $Id$
#Maintainer: Xyne <ac xunilhcra enyx, backwards>
pkgname=obtheme
pkgver=2011.04.04.1
pkgrel=1
pkgdesc="A GUI theme editor for Openbox."
depends=('python2' 'pygtk' 'gtk2' 'python-fuse')
arch=('any')
license=('GPL')
url="http://xyne.archlinux.ca/projects/${pkgname}"
source=(http://xyne.archlinux.ca/src/${pkgname}-${pkgver}.tar.gz)
md5sums=('cbafe552a949a3922d20e784db3dba2b')

build() {
  install -D -m755 "$srcdir/$pkgname/$pkgname" "$pkgdir/usr/bin/$pkgname"
  install -D -m644 "$srcdir/$pkgname/$pkgname.desktop" "$pkgdir/usr/share/applications/$pkgname.desktop"
}
# vim: set ts=2 sw=2 et:
