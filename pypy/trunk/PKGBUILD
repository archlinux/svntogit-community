# $Id$
# Maintainer: Sven-Hendrik Haase <sh@lutzhaase.com>

pkgname=pypy
pkgver=1.4
pkgrel=1
pkgdesc="A Python implementation written in Python, JIT enabled"
url="http://codespeak.net/pypy/"
arch=('i686' 'x86_64')
depends=('python2' 'libffi')
optdepends=('openssl: openssl module'
            'expat: pyexpat module'
            'ncurses: ncurses module'
            'zlib: zlib module'
            'bzip2: bz2 module')
license=('custom:MIT')
source=(LICENSE http://pypy.org/download/${pkgname}-${pkgver}-src.tar.bz2)
md5sums=('a6ed60c010618fd5bb8bad3570c5b8bd'
         '6c7e5a3fab4b3f6357aab84927420b49')

build() {
  cd ${srcdir}/${pkgname}-${pkgver}

  #cf="`pkg-config --cflags libffi`" || return 1
  #sed "s|cflags = \[|cflags = \[\'${cf/  /}\', |g" -i pypy/translator/platform/linux.py || return 1

  cd ${srcdir}/${pkgname}-${pkgver}/pypy/translator/goal
  python2 translate.py -Ojit targetpypystandalone.py
  install -Dm755 pypy-c ${pkgdir}/opt/pypy/pypy-c
  mkdir -p ${pkgdir}/opt/pypy/{lib-python,pypy}
  cp -r ${srcdir}/${pkgname}-${pkgver}/lib-python/2.5.2 ${pkgdir}/opt/pypy/lib-python/
  cp -r ${srcdir}/${pkgname}-${pkgver}/lib-python/modified-2.5.2 ${pkgdir}/opt/pypy/lib-python/
  cp -r ${srcdir}/${pkgname}-${pkgver}/lib_pypy ${pkgdir}/opt/pypy/
  mkdir -p ${pkgdir}/usr/bin
  ln -s /opt/pypy/pypy-c ${pkgdir}/usr/bin/pypy

  install -Dm644 ${srcdir}/LICENSE ${pkgdir}/usr/share/licenses/pypy/LICENSE
}
# vim: ts=2 sw=2 et:
