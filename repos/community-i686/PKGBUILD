# $Id$
# Maintainer: Geoffroy Carrier <geoffroy.carrier@koon.fr>

pkgname=qbittorrent
pkgver=1.3.3
pkgrel=3
pkgdesc="A bittorrent client written in C++ / Qt4 using the good libtorrent library"
arch=('i686' 'x86_64')
url="http://www.qbittorrent.org/"
license=('GPL')
depends=('qt' 'libtorrent-rasterbar>=0.14.2' 'curl')
optdepends=('zziplib: zip file extraction')
source=(http://downloads.sourceforge.net/sourceforge/qbittorrent/$pkgname-$pkgver.tar.gz)
md5sums=('863f963beefc3c93dfe25ee6a6b0ed47')

build() {
	cd "$srcdir/$pkgname-$pkgver"
	./configure --prefix=/usr || return 1
	make || return 1
	make INSTALL_ROOT="$pkgdir" install || return 1
}
