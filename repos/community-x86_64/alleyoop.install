pkgname=alleyoop

post_install() {
  usr/sbin/gconfpkg --install ${pkgname}
}

pre_remove() {
  usr/sbin/gconfpkg --uninstall ${pkgname}
}

pre_upgrade() {
  pre_remove $1
}

post_upgrade() {
  post_install $1
}

# vim:set ts=2 sw=2 et:
