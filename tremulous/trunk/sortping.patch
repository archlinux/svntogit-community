Index: src/client/cl_ui.c
===================================================================
--- src/client/cl_ui.c	(revision 1088)
+++ src/client/cl_ui.c	(working copy)
@@ -449,6 +449,7 @@
 			}
 			break;
 		case SORT_PING:
+		case 4:
 			if (server1->ping < server2->ping) {
 				res = -1;
 			}
