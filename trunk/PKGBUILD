# $Id$
# Maintainer: Alexander Rødseth <rodseth@gmail.com>

pkgname=setconf
pkgver=0.5.2
pkgrel=1
pkgdesc="Utility to easily change settings in Makefiles and configuration files"
arch=('any')
url="http://setconf.roboticoverlords.org/"
license=('GPL')
depends=('python')
#depends=('pcre' 'gc')
#makedepends=('shedskin')
source=("http://setconf.roboticoverlords.org/$pkgname-$pkgver.tbz2")
sha256sums=('3225a5392becbe6d679075be9ddd78e2c737d8158920c2f242f95d0765cfb250')

#build() {
#  cd "$srcdir/$pkgname-$pkgver"
#  shedskin "$pkgname.py"
#  make CPPFLAGS="-march=native -fomit-frame-pointer -fpermissive"
#}

package() {
  cd "$srcdir/$pkgname-$pkgver"

  install -Dm755 "$pkgname.py" "$pkgdir/usr/bin/$pkgname"
  install -Dm644 "$pkgname.1.gz" "$pkgdir/usr/share/man/man1/$pkgname.1.gz"
}

# vim:set ts=2 sw=2 et:
