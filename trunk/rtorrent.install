post_upgrade() {
  if [ `vercmp $2 0.7.7` -eq -1 ]; then
    echo "!!> rtorrent has changed it's rc file syntax. Update your   <!!"
    echo "!!> .rtorrent.rc to change anything with get_d_* to d.get_* <!!"
  fi
}
