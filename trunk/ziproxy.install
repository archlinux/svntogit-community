post_install() {
  mkdir -p /var/log/ziproxy && \
  chown -R root:proxy /var/log/ziproxy && \
  chmod -R ug+w /var/log/ziproxy
}

post_upgrade() {
  mkdir -p /var/log/ziproxy && \
  chown -R root:proxy /var/log/ziproxy && \
  chmod -R ug+w /var/log/ziproxy
}
