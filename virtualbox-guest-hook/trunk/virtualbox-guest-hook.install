post_install() {
  echo "Please add 'vboxguest' to the HOOKS array in /etc/mkinitcpio.conf, and 'linux-headers' or any custom kernel headers to the SyncFirst array in /etc/pacman.conf to enable the automatic module compilation."
}

# vim: ts=2 sw=2 et:
